
namespace CryptoPortfolioTracker.Enums;

public enum GraphStatus
{
    Idle = 0,
    Updating = 1,
    Modifying = 2,
}

public enum HeatMapStatus
{
    Idle = 0,
    Updating = 1,
    Modifying = 2,
}
